# NO USAR — datos heredados de pir

Todo lo que hay en esta carpeta es contenido real de **pir.turel.es** (convocatorias
de Psicología 2021-2025 y su clasificación en 58 temas), heredado sin querer al
clonar el historial de git de `ia4you/pir` en `ia4you/fir`.

No corresponde a FIR (Farmacia) y **no debe ejecutarse nunca contra `fir-db`**.

Se conserva aquí solo como referencia de formato (cómo son un `insert_AAAA.sql`,
un `update_tema.sql`, etc.) para cuando llegue la ingesta real de las 5
convocatorias de Farmacia. Bórrala cuando ya no la necesites como plantilla.
